/*This will be the adress used for the backend if it is deployed you need to enter the url here if it is
running locally (on the port 3000) you can leave the current one*/
export default url = "http://localhost:3000";
